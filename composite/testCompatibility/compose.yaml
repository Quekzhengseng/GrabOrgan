version: "3.8"

services:
  test-compatibility:
    build:
      context: .
      dockerfile: Dockerfile.testCompatibility
    container_name: "test-compatibility"
    environment:
      - RABBITMQ_HOST=rabbitmq
    networks:
      - grabOrgan-net
    ports:
      - "5022:5022"

networks:
  grabOrgan-net:
    external: true
